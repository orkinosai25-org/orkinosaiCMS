@page "/"
@inject NavigationManager NavigationManager

<PageTitle>OrkinosaiCMS</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to the branded website home page
        NavigationManager.NavigateTo("/cms-home", true);
    }
}
